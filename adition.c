#include <stdio.h>

/**
 * main -addition
 * Return: Always 0
 */

int main(void)
{
	int a, b;

	printf("enter number\n");
	scanf("%d ", &a);
	printf("enter number\n");
	scanf("%d ", &b);
	printf("%d + %d = %d\n", a, b, a + b);

	return (0);
}
